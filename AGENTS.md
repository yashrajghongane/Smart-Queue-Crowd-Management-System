# Contribution Guidelines

Keep backend, frontend, firmware, documentation, and tests in their respective directories. Keep secrets in local `.env` files and update `.env.example` when configuration changes.
